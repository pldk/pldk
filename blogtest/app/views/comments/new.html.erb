<h1>New Comment</h1>

<%= render 'form' %>

<%= link_to 'Back', post_comments_path(@comment.post) %>


<p>
  <b>Body:</b>
  <%= @comment.body %>
</p>


<!-- /posts/:post_id/comments/ -->
<%= link_to 'Back', post_comments_path(@comment.post) %>